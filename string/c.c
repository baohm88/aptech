#include <stdio.h>
#include<string.h>


int main(void)
{
    char a, str[81], *p;

    printf("\nYour sentence: ");
    gets(str)
    printf("\nEnter a char to search for: ");
    a = getchar();

    p = strchr(str, a);
    printf("\nString address: %")
}
